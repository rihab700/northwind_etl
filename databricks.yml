# This file defines the structure of this project and how it is deployed
# to production using Databricks Asset Bundles.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: northwind
  uuid: 673b76d2-e451-455e-828a-be567f03c147

include:
  - resources/*.yml
  - resources/*/*.yml

# Deployment targets.
# The default schema, catalog, etc. for dbt are defined in dbt_profiles/profiles.yml
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-a48ea08f-0f7b.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-a48ea08f-0f7b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nadia.errasfatou73@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nadia.errasfatou73@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: nadia.errasfatou73@gmail.com
        level: CAN_MANAGE
